#include <stdio.h>

int main() {
    int size = 4;
    char marks[size];
    int i;

    for (i = 0; i < size; i++) {
        scanf("%c", &marks[i]);
    }
    for(i = 0;i<size; i++){
        printf("The element at %d is %c\n",i,marks[i]);
    }

    return 0;
}
