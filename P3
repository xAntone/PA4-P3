#include <stdio.h>

int main() {
    int size = 4;
    int marks[size];
    int i;

    for (i = 0; i < size; i++) {
        scanf("%d", &marks[i]);
    }
    for(i = 0;i<size; i++){
        printf("The element at %d is %d\n",i,marks[i]);
    }

    return 0;
}
